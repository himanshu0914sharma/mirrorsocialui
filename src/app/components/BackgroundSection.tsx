import React from 'react';

const BackgroundSection: React.FC = () => {
  return (
    <section className="relative h-screen flex items-center justify-center bg-cover bg-center bg-blur">
      <div className="text-center">
        <h1 className="text-4xl font-bold text-white">Mirror Social</h1>
      </div>
    </section>
  );
};

export default BackgroundSection;
